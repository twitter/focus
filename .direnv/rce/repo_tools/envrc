
if [[ -z "${PYENV_ROOT:-}" && -c "$HOME/.pyenv" ]]; then
  export PYENV_ROOT="$HOME/.pyenv"
fi

if [[ -n "${PYENV_ROOT:-}" ]]; then
  PATH_add "$PYENV_ROOT/bin"
fi

export PYENV_VERSION=3.9.4
layout pipenv
PATH_add "$(expand_path ../twgit-utils/dist)"

# vim:ft=bash:ts=2:sw=2:et
